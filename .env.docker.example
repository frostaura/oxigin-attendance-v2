# Docker environment variables for production
# Copy this file to .env.docker and replace with your actual values

ASPNETCORE_ENVIRONMENT=Production
ConnectionStrings__DefaultConnection=Host=postgres;Database=oxigin_attendance;Username=postgres;Password=YOUR_POSTGRES_PASSWORD
JwtSettings__SecretKey=YOUR_JWT_SECRET_KEY_AT_LEAST_32_CHARACTERS_LONG
REACT_APP_API_URL=http://localhost:5000/api